/**
 * Một số chủ đề đã học:
 * - Cầu điều kiện rẽ nhánh if...else và switch...case
 * - Xử lý số
 * - Xử lý chuỗi
 * - Xử lý mảng 
 */

/**
 * Các kĩ thuật đã học:
 * - Khử nested level bằng if...return
 * - Dùng toán tử 3 ngôi cho phép gán có điều kiện
 * - Kỹ thuật đặt cờ hiệu (true/false)
 * - Kỹ thuật đặt lính canh
 */

/**
 * Cấu trúc của một function
 */
function doSomething(a, b) {
 // 1. Check a, b. if not valid then return
 // 2. Processing
 // 3. Return the result (optional)
}

/**
 * Các nguồn bài tập
 * 1000 bài tập (chỉ làm chương 1 -> chương 5): https://nguyenvanhieu.vn/1000-bai-tap-lap-trinh/#google_vignette 
 * Các trang luyện code online: edabit, leetcode, hackerrank, ...
 */