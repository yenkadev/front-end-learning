/**
 * Kiểu dữ liệu
 * Prirmitive: number, boolean, string, null, undefined, symbol
 * Non-Primitive: object, array
 */

const age = 18;
const name = "Yen";
const isActive = true;
const student = {
  name: "Dong",
  age: 22
} 
let count; // Khai báo bằng const phải có giá trị khởi tạo


